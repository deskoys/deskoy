{
  "name": "deskoy-relay",
  "private": true,
  "version": "1.0.0",
  "description": "Server-side relay: Deskoy app → Discord webhooks (secrets stay on Vercel)",
  "scripts": {
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vercel/node": "^3.2.0",
    "typescript": "~5.4.5"
  },
  "engines": {
    "node": ">=18"
  }
}
